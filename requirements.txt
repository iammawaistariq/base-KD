# Stable CUDA 12.1 stack used by the Vision-Mamba backend.
# Install in a Python 3.10 env. For mamba-ssm, install CUDA nvcc/g++ first,
# then run the README mamba install command with --no-build-isolation.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.4.1+cu121
torchvision==0.19.1+cu121
timm==1.0.27
PyYAML==6.0.3
tqdm==4.68.4
numpy==2.2.6
scikit-learn==1.7.2
pytest==9.1.1
ruff==0.15.20

# mamba-ssm 2.2.x imports generation symbols that are removed/changed in
# newer Transformers releases. Keep this pinned before installing mamba-ssm.
transformers==4.44.2
tokenizers==0.19.1
huggingface-hub==0.36.2

# Build helpers used by causal-conv1d / mamba-ssm source builds.
ninja==1.13.0
einops==0.8.2
packaging==26.0
